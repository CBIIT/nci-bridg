import { NCISiteAlertOptions } from './nci-site-alert-options';
export declare class NCISiteAlert {
    protected element: HTMLElement;
    protected options: NCISiteAlertOptions;
    private closeButton;
    private collapse;
    private static optionDefaults;
    private static _components;
    protected constructor(element: HTMLElement, options?: Partial<NCISiteAlertOptions>);
    static create(element: HTMLElement, options?: Partial<NCISiteAlertOptions>): NCISiteAlert;
    static autoInit(): void;
    unregister(): void;
    private initialize;
    private createCloseButton;
    private createCollapse;
}
