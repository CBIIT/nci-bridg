import { FocusTrap } from '../focus-trap';
import { MobileMenuAdaptor } from './mobile-menu-adaptor';
import { MobileMenuData } from './mobile-menu-data';
import { MobileMenuItem } from './mobile-menu-item';
export declare class MobileMenu {
    protected readonly element: HTMLElement;
    private readonly adaptor;
    protected focusTrap: FocusTrap;
    protected activeMenu: boolean;
    protected menuData: MobileMenuData | null;
    protected mobileNav: HTMLElement;
    protected mobileOverlay: HTMLElement;
    protected mobileButton: HTMLElement;
    protected mobileClose: HTMLElement;
    protected pageUrl: string;
    protected sectionParent: MobileMenuItem | null;
    protected langCode: 'en' | 'es';
    protected loader: Element | HTMLElement;
    private resizeMediaQuery;
    protected resizeWidth: number;
    private customEvents;
    private linkClickListener;
    private menuOpenEventListener;
    private windowResizeEventListener;
    private menuCloseEventListener;
    private escapeKeyPressListener;
    constructor(mobileNavElement: HTMLElement, adaptor: MobileMenuAdaptor);
    unregister(): void;
    private initialize;
    private handleOpenMenu;
    private handleCloseMenu;
    private openMenu;
    private closeMenu;
    private handleLinkClick;
    private displayNavLevel;
    private makeBackNode;
    private makeMenuNode;
    private makeMenuLink;
    private createDom;
    private createCustomEvents;
}
