import { MobileMenuAdaptor } from './mobile-menu-adaptor';
import { MobileMenuData } from './mobile-menu-data';
import { MobileMenuItem } from './mobile-menu-item';
export declare class DefaultMobileMenuSource implements MobileMenuAdaptor {
    useUrlForNavigationId: boolean;
    primaryNavItems: HTMLElement[];
    items: MobileMenuItem[];
    lang: string;
    constructor();
    getInitialMenuId(): Promise<string | number>;
    getNavigationLevel(id: number | string): Promise<MobileMenuData>;
    createDefaultMobileMenu(id: string | number): MobileMenuData;
}
