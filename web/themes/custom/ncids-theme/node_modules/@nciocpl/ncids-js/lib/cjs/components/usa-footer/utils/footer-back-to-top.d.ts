export declare class NCIBackToTop {
    protected element: HTMLElement;
    private scrollListener;
    constructor(element: HTMLElement);
    private handleScroll;
    unregister(): void;
    private initialize;
}
