import { SiteAlertCloseButtonOptions } from './site-alert-close-button-options';
export declare class SiteAlertCloseButton {
    protected element: HTMLElement;
    protected options: SiteAlertCloseButtonOptions;
    private readonly button;
    private customEvents;
    private eventListener;
    constructor(element: HTMLElement, options: SiteAlertCloseButtonOptions);
    unregister(): void;
    private initialize;
    private createButton;
    private hideSiteAlert;
    private addButton;
    private addEventListeners;
    private removeEventListeners;
    private handleClose;
    private createCustomEvents;
}
